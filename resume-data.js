const resumeData = {
  // Paste the entire JSON data object here from the previous response
  // (The data.json content goes here)
};

// For Node.js
if (typeof module !== 'undefined' && module.exports) {
  module.exports = resumeData;
}
